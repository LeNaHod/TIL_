# 24년 및 평소 공부 자잘자잘

## 스크립트

### Window 관련 (cmd, powershell ..)

#### 날짜출력 및 파일 생성

%date ~ : 날짜출력
%time ~ : 시간출력
@file , forfile : 파일삭제 수정 등에 사용되는 커맨드

```bash
1. 현재날짜를 - 없이 출력
echo %date:-=%
20240816

2. 기본포맷으로 출력 (2024-08-16...)

echo %date%



```

### cmd에서 시스템 날짜 바꾸기

```bash

date
현재 날짜 : 2024-08-16
새로운 날짜를 입력하십시오: (년-월-일) : 2024-07-16

```
위의 작업을하려면 당연히 cmd나 powershell를 **관리자 권한으로 실행해야한다.**
또한, 위에서 다룬 날짜출력및 커맨드를 이용하여 파일생성을하거나 작업을하는것들은 모두 시스템 날짜 기준으로함
그렇기에 시스템 날짜를 바꾼 이후에 날짜 출력 커맨드를 치면 아래와 같은 결과가 반환된다. 그럼으로 시스템날짜를 잘 동기화시켜주거나 작업전에 확인해주면 좋다.

```bash

echo %date:-=%
20240716

```
